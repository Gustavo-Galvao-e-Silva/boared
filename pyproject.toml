[project]
name = "boared"
version = "0.1.0"
description = "Claude Code skills for building verified math study-session materials"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
  "panchi>=2.0.0",
  "python-pptx>=1.0",
  "matplotlib>=3.5",
  "pyyaml>=6.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
